# CHUNKER app icon

Place your final application icon files in this folder.

Current default icon:
- `chunker-icon.svg` (used as favicon in `index.html`)

If you replace the icon, keep the filename the same or update the `<link rel="icon">` path in `index.html`.
